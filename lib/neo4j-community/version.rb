module Neo4j
  module Community
    # dnagir: I don't think gem version should == NEO_VERSION
    # It allows updating gem without neo4j itself
    VERSION = "0.0.1.alpha"
    NEO_VERSION = "1.6.M02"
  end
end
